---
name: Input
---

import { Playground, Props } from "docz";
import { Input } from "../src";
import { Field } from "./Field";

# Input

Renders a `Input` input that will values.

## Basic usage

<Playground>
  <Field initialValue="">
    {(value, onChange) => <Input value={value} onChange={onChange} />}
  </Field>
</Playground>

## Props

<Props of={Input} />
