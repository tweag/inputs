---
name: Input
---

import { Playground, Props } from "docz";
import { Input } from "../src";
import { Field } from "./Field";

# Input

Renders an `input` that emits the `value` on change.

## Basic usage

<Playground>
  <Field initialValue="">
    {(value, onChange) => <Input value={value} onChange={onChange} />}
  </Field>
</Playground>

## Props

<Props of={Input} />
