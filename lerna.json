{
  "version": "0.6.0",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "allowBranch": "master",
      "ignoreChanges": [
        "!packages/**"
      ],
      "message": "Release %s 🚀 [skip ci]"
    }
  }
}
