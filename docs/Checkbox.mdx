---
name: Checkbox
---

import { Playground, Props } from "docz";
import { Checkbox } from "../src";
import { Field } from "./Field";

# Checkbox

Renders an `input[type=checkbox]` that will emit boolean values on change.

## Basic usage

<Playground>
  <Field initialValue={true}>
    {(value, onChange) => <Checkbox value={value} onChange={onChange} />}
  </Field>
</Playground>

## Props

<Props of={Checkbox} />
